@model Estoque.Models.ItensEstoque

@{
    ViewData["Title"] = "Estoque";
    Layout = "~/Views/Shared/_Layout.cshtml";
}
<!DOCTYPE html>

<html lang="en">
<head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <title>Create</title>
</head>
<body>

    <h4>ItensEstoque</h4>
    <hr />
    <h2>Create</h2>

    <div class="row container">
        <div class="col-md-4 box">
            <form asp-action="Create">
                <div asp-validation-summary="ModelOnly" class="text-danger mb-3"></div>

                <div class="mb-3">
                    <label asp-for="Nome" class="form-label">Nome</label>
                    <input asp-for="Nome" class="form-control" />
                </div>

                <div class="mb-3">
                    <label asp-for="Setor" class="form-label">Setor</label>
                    <input asp-for="Setor" class="form-control" />
                </div>

                @* <div class="mb-3">
                    <label asp-for="Setor" class="form-label">Setor</label>
                    <select asp-for="Setor" asp-items="Html.GetEnumSelectList<SetorEnum>()" class="form-control">
                        <option value="" disabled selected>Selecione o setor</option>
                    </select>
                </div> *@

                <div class="mb-3">
                    <label asp-for="Quantidade" class="form-label">Quantidade</label>
                    <input asp-for="Quantidade" class="form-control" />
                    <span asp-validation-for="Quantidade" class="text-danger"></span>

                </div>

                <div class="form-group">
                    <input type="submit" value="Create" class="btn btn-primary" />
                    <a class="btn btn-primary" asp-action="Index">Voltar</a>
                </div>
            </form>
        </div>

</body>
</html>
